<div class="home-thanh-travels">
  <div class="horizontal-scroll">
    <%= link_to location_path(@featured_locations[0].url) do %>
      <%= image_tag ("#{@featured_locations[0].url}-banner.JPG"), class: "scroll one" %>
    <% end %>

    <%= link_to location_path(@featured_locations[1].url) do %>
      <%= image_tag ("#{@featured_locations[1].url}-banner.JPG"), class: "scroll two" %>
    <% end %>

    <div class="scroll three"></div>
    <div class="scroll four"></div>
  </div>
</div>
