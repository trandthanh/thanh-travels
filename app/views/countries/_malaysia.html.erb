<div class="country-show">
  <div class="horizontal-scroll">
    <% @country_locations.each do |l| %>
      <div class="banner">
        <%= link_to location_path(l.url) do %>
          <%= image_tag ("#{l.url}-baner.jpg"), class: "scroll one" %>
          <div class="overlay">
            <p><%= l.location_name %></p>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>

